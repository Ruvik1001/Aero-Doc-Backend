# Aero Doc Backend

Система для обработки документов с использованием RAG (Retrieval-Augmented Generation) на базе Milvus, GigaChat и FastAPI.

## 📋 Описание

Aero Doc Backend — это сервис для загрузки, обработки и поиска информации в PDF документах. Система использует векторный поиск (Milvus) для индексации документов и генерацию ответов через GigaChat API на основе найденных релевантных фрагментов.

### Основные возможности

- 📄 **Загрузка PDF документов** — массовая загрузка через API
- 🔍 **Векторный поиск** — семантический поиск по документам с использованием embeddings
- 🤖 **Генерация ответов** — автоматическое формирование ответов на основе найденных фрагментов через GigaChat
- 🚀 **Асинхронная обработка** — обработка документов в фоновом режиме
- 📊 **Мониторинг** — JSON логирование всех операций

## 🏗️ Архитектура

Система состоит из следующих компонентов:

- **FastAPI приложение** (`proxy`) — основной API сервис
- **Milvus** — векторная база данных для хранения embeddings
- **etcd** — хранилище метаданных для Milvus
- **MinIO** — объектное хранилище для Milvus
- **GigaChat API** — генерация ответов на основе контекста
- **Nginx** — обратный прокси (опционально)
- **Attu** — веб-интерфейс для управления Milvus (опционально)

### Технологический стек

- **Backend**: FastAPI, Python 3.11
- **Vector DB**: Milvus v2.3.12
- **Embeddings**: SentenceTransformer (`intfloat/multilingual-e5-large-instruct`)
- **LLM**: GigaChat API
- **Document Processing**: LangChain, PDFPlumber
- **Containerization**: Docker, Docker Compose

## 📦 Требования

- Docker 20.10+
- Docker Compose 2.0+
- Python 3.11+ (для локального запуска скриптов)
- Минимум 8 GB RAM (рекомендуется 16 GB)
- Минимум 20 GB свободного места на диске

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd "Aero Doc Backend"
```

### 2. Настройка переменных окружения

Создайте файл `proxy/.env`:

```env
# GigaChat API ключ
GIGA_KEY=your_gigachat_api_key_here

# Директория для хранения документов (внутри контейнера)
DOC_DIR=/app/docs
```

### 3. Запуск сервисов

```bash
docker compose up -d --build
```

Это запустит все необходимые сервисы:
- `proxy` — API сервис (порт 10000)
- `standalone` — Milvus (порт 19530)
- `etcd` — хранилище метаданных
- `minio` — объектное хранилище (порты 9000, 9001)
- `attu` — веб-интерфейс Milvus (порт 3000)
- `nginx` — обратный прокси (порты 80, 443)

### 4. Проверка работоспособности

```bash
# Проверка здоровья API
curl http://127.0.0.1:10000/api/v1/health

# Проверка статуса контейнеров
docker compose ps
```

## 📚 Использование API

### Базовый URL

```
http://127.0.0.1:10000/api/v1/chat
```

### Эндпоинты

#### 1. Загрузка документов

**POST** `/upload`

Загружает один или несколько PDF файлов для обработки.

**Запрос:**
```bash
curl -X POST "http://127.0.0.1:10000/api/v1/chat/upload" \
  -F "files=@document1.pdf" \
  -F "files=@document2.pdf"
```

**Ответ:**
```json
{
  "success": true,
  "message": "Successfully uploaded 2 document(s). Processing started in background.",
  "filename": "document1.pdf, document2.pdf",
  "file_path": "/app/docs"
}
```

**Параметры:**
- `files` (multipart/form-data) — один или несколько PDF файлов
- Максимальный размер файла: 50 MB
- Обработка выполняется асинхронно в фоновом режиме

#### 2. Поиск и генерация ответа

**POST** `/q`

Выполняет семантический поиск по документам и генерирует ответ на основе найденных фрагментов.

**Запрос:**
```bash
curl -X POST "http://127.0.0.1:10000/api/v1/chat/q" \
  -H "Content-Type: application/json" \
  -d '{
    "request": "Какой максимальный вес груза разрешен для перевозки?"
  }'
```

**Ответ:**
```json
{
  "request": "Какой максимальный вес груза разрешен для перевозки?",
  "response": "Согласно документации, максимальный вес груза составляет 25 тонн...",
  "onTextBased": [
    {
      "text": "Максимальный вес груза не должен превышать 25 тонн...",
      "source": "document1.pdf"
    },
    {
      "text": "При перевозке грузов необходимо учитывать...",
      "source": "document2.pdf"
    }
  ]
}
```

**Параметры:**
- `request` (string) — вопрос пользователя

**Ответ содержит:**
- `response` — сгенерированный ответ через GigaChat
- `onTextBased` — список релевантных фрагментов из документов с указанием источника

#### 3. Проверка здоровья

**GET** `/api/v1/health`

Проверяет статус сервиса.

**Ответ:**
```json
{
  "status": "healthy"
}
```

### Документация API

Интерактивная документация доступна по адресам:
- Swagger UI: `http://127.0.0.1:10000/docs`
- ReDoc: `http://127.0.0.1:10000/redoc`

## 🛠️ Утилиты

### Массовая загрузка документов

Используйте скрипт `upload_files.py` для загрузки всех PDF файлов из папки:

```bash
python upload_files.py --folder td --api-url http://127.0.0.1:10000/api/v1/chat/upload
```

**Параметры:**
- `--folder` — путь к папке с PDF файлами (по умолчанию: `td`)
- `--api-url` — URL эндпоинта загрузки (по умолчанию: `http://127.0.0.1:10000/api/v1/chat/upload`)

**Пример:**
```bash
# Загрузка всех PDF из папки td
python upload_files.py

# Загрузка из другой папки
python upload_files.py --folder /path/to/documents

# Использование другого API URL
python upload_files.py --api-url http://localhost:8080/api/v1/chat/upload
```

## 📁 Структура проекта

```
Aero Doc Backend/
├── docker-compose.yml          # Конфигурация Docker Compose
├── upload_files.py             # Скрипт для массовой загрузки документов
├── README.md                   # Документация
│
├── proxy/                      # Основное приложение
│   ├── main.py                 # Точка входа FastAPI
│   ├── Dockerfile              # Docker образ для приложения
│   ├── requirements.txt        # Python зависимости
│   ├── .env                    # Переменные окружения (создать вручную)
│   │
│   ├── router/                 # API роутеры
│   │   ├── chat.py             # Эндпоинты для чата и загрузки
│   │   └── health.py           # Эндпоинт проверки здоровья
│   │
│   ├── schema/                 # Pydantic схемы
│   │   └── chat.py             # Модели запросов/ответов
│   │
│   └── utils/                  # Утилиты
│       ├── search.py           # Поиск и парсинг документов
│       ├── TextEncoder_impl.py # Модель для embeddings
│       ├── TextChunker_impl.py # Разбиение документов на чанки
│       ├── MilvusSingleton_impl.py # Подключение к Milvus
│       └── giga.py             # Интеграция с GigaChat
│
├── nginx/                      # Nginx конфигурация
│   ├── Dockerfile
│   ├── nginx.conf
│   └── ssl/                    # SSL сертификаты
│
└── td/                         # Папка с документами для загрузки
    └── *.pdf                   # PDF файлы
```

## ⚙️ Конфигурация

### Переменные окружения

#### `proxy/.env`

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `GIGA_KEY` | API ключ для GigaChat | Да | - |
| `DOC_DIR` | Путь к директории с документами в контейнере | Нет | `/app/docs` |

### Docker Compose переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DOCKER_VOLUME_DIRECTORY` | Базовый путь для томов Docker | `.` (текущая директория) |

### Параметры модели

Модель embeddings: `intfloat/multilingual-e5-large-instruct`
- Размерность векторов: 1024
- Поддержка: автоматическое определение GPU/CPU
- Первая загрузка: ~15-20 минут на CPU

### Параметры обработки документов

- **Размер чанка**: 1200 символов (настраивается в `TextChunker_impl.py`)
- **Перекрытие чанков**: 200 символов
- **Максимальный размер файла**: 50 MB
- **Формат**: только PDF
- **Количество релевантных фрагментов для ответа**: 15 (настраивается в `search.py`)

## 🔧 Разработка

### Локальная разработка

1. Установите зависимости:

```bash
cd proxy
pip install -r requirements.txt
```

2. Настройте переменные окружения (создайте `proxy/.env`)

3. Запустите только инфраструктуру (Milvus, etcd, MinIO):

```bash
docker compose up -d etcd minio standalone
```

4. Запустите приложение локально:

```bash
cd proxy
uvicorn proxy.main:app --host 0.0.0.0 --port 8080 --reload
```

### Пересборка контейнеров

```bash
# Пересборка всех сервисов
docker compose up -d --build

# Пересборка только proxy
docker compose up -d --build request_manager_service_crm
```

### Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Только proxy
docker compose logs -f proxy

# Последние 100 строк
docker compose logs --tail=100 proxy
```

## 🐛 Troubleshooting

### Проблема: Контейнер proxy не запускается

**Решение:**
1. Проверьте логи: `docker compose logs proxy`
2. Убедитесь, что файл `proxy/.env` существует и содержит `GIGA_KEY`
3. Проверьте, что порт 10000 не занят другим процессом

### Проблема: Модель загружается очень долго

**Причина:** Модель `multilingual-e5-large-instruct` большая (~2-3 GB) и первая загрузка может занять 15-20 минут на CPU.

**Решение:**
- Это нормальное поведение при первом запуске
- Модель кэшируется после первой загрузки
- Используйте GPU для ускорения (если доступен)

### Проблема: Ошибка подключения к Milvus

**Решение:**
1. Убедитесь, что контейнер `standalone` запущен: `docker compose ps`
2. Проверьте логи Milvus: `docker compose logs standalone`
3. Дождитесь полной инициализации Milvus (может занять 1-2 минуты)

### Проблема: Документы не обрабатываются

**Решение:**
1. Проверьте логи обработки: `docker compose logs proxy | grep parser`
2. Убедитесь, что файлы сохранены в `/app/docs` внутри контейнера
3. Проверьте, что PDF файлы не повреждены и не пустые

### Проблема: Ошибка "Permission denied" в Milvus/etcd

**Решение:**
- Используются именованные тома Docker, проблема должна быть решена
- Если проблема сохраняется, проверьте права доступа к томам:
  ```bash
  docker volume inspect aerodocbackend_milvus_data
  docker volume inspect aerodocbackend_etcd_data
  ```

### Проблема: GigaChat возвращает ошибку

**Решение:**
1. Проверьте валидность `GIGA_KEY` в `proxy/.env`
2. Убедитесь, что у вас есть доступ к GigaChat API
3. Проверьте логи: `docker compose logs proxy | grep giga`

## 📊 Мониторинг

### Веб-интерфейсы

- **Attu (Milvus UI)**: http://127.0.0.1:3000
- **MinIO Console**: http://127.0.0.1:9001 (логин: `minioadmin`, пароль: `minioadmin`)
- **API Docs**: http://127.0.0.1:10000/docs

### Логирование

Все логи выводятся в формате JSON для удобного парсинга:

```json
{
  "timestamp": "2024-01-01T12:00:00",
  "level": "INFO",
  "logger": "proxy.router.chat",
  "message": "Document uploaded successfully",
  "file_name": "document.pdf",
  "file_size": 1024000
}
```

### Проверка состояния

```bash
# Статус всех контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка здоровья Milvus
curl http://127.0.0.1:9091/healthz
```

## 🔒 Безопасность

- API доступен только на `127.0.0.1:10000` (локально)
- Для продакшена настройте Nginx с SSL
- Храните `GIGA_KEY` в секретах, не коммитьте в репозиторий
- Используйте `.env` файлы, которые добавлены в `.gitignore`

---

**Примечание:** При первом запуске система загрузит модель embeddings, что может занять 15-20 минут. Последующие запуски будут быстрее благодаря кэшированию.

